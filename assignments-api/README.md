# Assignments API

This directory is a basic example of using the Express API framework.
Here we've setup two basic routes which follow some of the REST principles.

- `GET /assignments`
  - Returns all available assignments
- `GET /assignments/<id>`
  - Returns details about an individual assignment specified by its ID
